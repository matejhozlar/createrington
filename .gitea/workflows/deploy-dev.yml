name: Deploy Dev

on:
  push:
    branches: [dev]
  workflow_dispatch:
    inputs:
      reset_db:
        description: 'Reset the database (drop + recreate + seed)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  deploy:
    runs-on: hetzner
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Generate types, command docs, and deploy commands
        run: |
          set -a
          source /opt/createrington-dev/.env
          set +a
          export DB_HOST=infra-postgres
          export DB_PORT=5432
          pnpm generate:ci
          pnpm generate-command-docs:json
          pnpm generate-api-docs
          pnpm deploy-commands

      - name: Check for API spec changes
        id: spec-check
        run: |
          if git diff ${{ github.event.before }}..HEAD --name-only -- '*.api-spec.ts' | grep -q .; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup JDK 21
        if: steps.spec-check.outputs.changed == 'true'
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        if: steps.spec-check.outputs.changed == 'true'
        uses: gradle/actions/setup-gradle@v4

      - name: Generate and publish mod API (dev)
        if: steps.spec-check.outputs.changed == 'true'
        run: |
          pnpm generate-mod-api
          cd mod-api
          sed -i 's/artifact_id=createrington-api/artifact_id=createrington-dev-api/' gradle.properties
          VERSION=$(grep '^version=' gradle.properties | cut -d'=' -f2)
          JAR_URL="https://raw.githubusercontent.com/matejhozlar/maven/main/com/saunhardy/createrington-dev-api/${VERSION}/createrington-dev-api-${VERSION}.jar"
          if curl --silent --head --fail "$JAR_URL" > /dev/null 2>&1; then
            echo "ERROR: createrington-dev-api:${VERSION} already published. Bump version in mod-api/gradle.properties"
            exit 1
          fi
          gradle publish
          cd ..
          git clone https://x-access-token:${{ secrets.MAVEN_REPO_TOKEN }}@github.com/matejhozlar/maven.git maven-repo
          cp -r mod-api/build/maven-publish/. maven-repo/
          cd maven-repo
          git config user.email "ci@createrington.com"
          git config user.name "CI"
          git add .
          git diff --cached --quiet || git commit -m "publish createrington-dev-api ${VERSION}"
          git push

      - name: Build
        run: |
          VITE_APP_VERSION="$(node -p 'require("./package.json").version')-dev.$(git rev-parse --short HEAD)" \
          VITE_API_URL=https://dev.create-rington.com \
          VITE_BLUEMAP_URL=https://map-test.create-rington.com \
          pnpm build

      - name: Deploy frontend
        run: |
          rm -rf /var/www/createrington-dev/*
          cp -r dist/public/* /var/www/createrington-dev/

      - name: Deploy backend
        run: |
          rm -rf /opt/createrington-dev/dist
          cp -r dist /opt/createrington-dev/dist
          cp package.json /opt/createrington-dev/package.json
          rm -rf /opt/createrington-dev/node_modules
          cp -r node_modules /opt/createrington-dev/node_modules
          mkdir -p /opt/createrington-dev/packages/server
          rm -rf /opt/createrington-dev/packages/server/node_modules
          cp -r packages/server/node_modules /opt/createrington-dev/packages/server/node_modules

      - name: Migrate database
        if: ${{ github.event.inputs.reset_db != 'true' }}
        run: |
          export DB_HOST=infra-postgres
          export DB_PORT=5432
          export DB_USER=$(grep '^DB_USER=' /opt/createrington-dev/.env | cut -d'=' -f2-)
          export DB_PASSWORD=$(grep '^DB_PASSWORD=' /opt/createrington-dev/.env | cut -d'=' -f2-)
          export DB_DATABASE=$(grep '^DB_DATABASE=' /opt/createrington-dev/.env | cut -d'=' -f2-)
          export PGPASSWORD=$DB_PASSWORD
          pnpm db:migrate

      - name: Reset database
        if: ${{ github.event.inputs.reset_db == 'true' }}
        run: |
          apt-get update && apt-get install -y postgresql-client > /dev/null 2>&1
          export DB_HOST=infra-postgres
          export DB_PORT=5432
          export DB_USER=$(grep '^DB_USER=' /opt/createrington-dev/.env | cut -d'=' -f2-)
          export DB_PASSWORD=$(grep '^DB_PASSWORD=' /opt/createrington-dev/.env | cut -d'=' -f2-)
          export DB_DATABASE=$(grep '^DB_DATABASE=' /opt/createrington-dev/.env | cut -d'=' -f2-)
          export PGPASSWORD=$DB_PASSWORD

          psql -h $DB_HOST -U "$DB_USER" -d postgres -c "DROP DATABASE IF EXISTS $DB_DATABASE WITH (FORCE);"
          psql -h $DB_HOST -U "$DB_USER" -d postgres -c "CREATE DATABASE $DB_DATABASE;"
          pnpm db:migrate
          psql -h $DB_HOST -U "$DB_USER" -d "$DB_DATABASE" -f db/data/test-data.sql

      - name: Fix permissions and trigger restart
        run: |
          rm -f /opt/createrington-dev/.maintenance
          chown -R 1000:988 /opt/createrington-dev
          chown -R 1000:988 /var/www/createrington-dev
          touch /opt/createrington-dev/.restart-trigger
