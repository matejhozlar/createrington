export const MODS_DIR = "mods";
export const CACHE_DIR = ".structure-pack-cache";

/** One week in seconds: the reference period for the time-factor weight component. */
export const T_REF = 7 * 24 * 60 * 60;

/** Fallback elapsed-weeks count applied to packs that have never been active. */
export const DEFAULT_ELAPSED_WEEKS = 4;
