{
  "name": "@createrington/shared",
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "pnpm clean && tsc",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.9.3"
  }
}
